# Profiles

UGTP conformance profiles define subsets of the standard that implementations may support.

## Planned Profiles

- **Minimal** — health + capabilities + single-chain transfer
- **Full** — all operations including cross-chain and smart accounts
- **Explorer** — read-only access to logical transactions and receipts

Status: Placeholder — profiles will be defined after the CANDIDATE standard stabilizes.
